pub mod components {
    #[derive(Debug)]
    pub struct Position1 {
        pub(crate) x: f32,
        pub(crate) y: f32,
    }
    pub struct Position2 {
        pub(crate) x: f32,
        pub(crate) y: f32,
    }
    pub struct Position3 {
        pub(crate) x: f32,
        pub(crate) y: f32,
    }
    pub struct Position4 {
        pub(crate) x: f32,
        pub(crate) y: f32,
    }
}
